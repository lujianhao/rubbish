// 自定义配置，参考 ./default/setting.config.js，需要自定义的属性在这里配置即可
module.exports = {
  theme: {
    color: '#2C8CF2',
    mode: 'light',
    success: '#52c41a',
    warning: '#faad14',
    error: '#f5222f'
  },
  fixedHeader: true,
  multiPage: true,
  hideSetting: true,
  fixedTabs: true,
  asyncRoutes: true,
  animate: {
    name: 'fade',
    direction: 'default'
  }
}
